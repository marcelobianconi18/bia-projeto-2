PORT=3001
GOOGLE_API_KEY=AIza...

# --- Meta Ads (Facebook) integration ---
# Add a long-lived access token or app access token here. Do NOT commit real tokens to git.
# You can set either `META_TOKEN` (preferred) or `META_ACCESS_TOKEN`.
# To verify account-scoped calls you must also set `META_ADS_ACCOUNT_ID` (numeric or act_ prefixed).
META_TOKEN=EAAX...REDACTED_LONG_LIVED_TOKEN
META_ACCESS_TOKEN=EAAX...REDACTED_LONG_LIVED_TOKEN
META_ADS_ACCOUNT_ID=1584016552909256

# Optional: meta business / pixel / dataset ids used by some flows
META_BUSINESS_ID=
META_PIXEL_ID=
META_DATASET_ID=
